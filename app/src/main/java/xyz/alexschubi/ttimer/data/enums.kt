package xyz.alexschubi.ttimer.data

enum class Category {
    purple, blue, green, yellow, orange, red
}
enum class NotificationStatus {
    new, pending, ignored, removed
}
enum class Tag{
    alpha, beta, gamma, delta
}
enum class Source{
    PC1, PC2, Mobile1, Mobile2
}